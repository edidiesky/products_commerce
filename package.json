{
  "scripts": {
    "dev": "concurrently \"cd ./server && npm run dev\" \"cd ./client && npm run dev\"",
    "build": "npm i vite @vitejs/plugin-react@3.1.0 && cd client && npm install --legacy-peer-deps && tsc && vite build",
    "start": "cd server && npm install && npm run server",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^3.1.0",
    "vite": "^4.4.9"
  }
}
